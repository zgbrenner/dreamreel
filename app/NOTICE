DREAMREEL — third-party notices
================================

This product includes original GLSL transition shaders whose interface follows the
gl-transitions specification.

  gl-transitions (https://github.com/gl-transitions/gl-transitions)
  License: MIT
  The gl-transitions collection and specification are released under the MIT License.
  DREAMREEL's transition shaders (app/src/render/transitions.ts — the full ~40-shader
  catalog, including the 2026-06-24 and 2026-07-02 expansions) are original
  implementations conforming to that specification; well-known transition concepts
  (page curl, iris, venetian bars, voronoi shatter, …) are reimplemented from the
  effect idea, and no shader source was copied verbatim.

This product includes 3D simplex-noise GLSL used for the film-grain treatment.

  webgl-noise (https://github.com/ashima/webgl-noise, https://github.com/stegu/webgl-noise)
  License: MIT
  Copyright (c) 2011 Ashima Arts; Copyright (c) 2011-2016 Stefan Gustavson.
  The `sn_snoise(vec3)` implementation in app/src/render/shaderNoise.ts is Ashima Arts /
  Stefan Gustavson's MIT-licensed 3D simplex noise (helpers prefixed `sn_`), used by the
  post-FX film grain (app/src/render/postfx.ts). Ship-safe.

Runtime dependencies and their licenses are enforced by the CI license check
(see Phase 3 / .github/workflows). Ship-safe licenses only: MIT, BSD, Apache-2.0,
Zlib, ISC, CC0, public domain, and CC-BY (with rendered attribution).

  three.js .......... MIT
  postprocessing .... Zlib
  tone.js ........... MIT
  zustand ........... MIT
  zod ............... MIT
  react / react-dom . MIT

Optional psychedelic layer — lazily code-split, engaged only behind the ?butterchurn=1
engine flag (default OFF). Both packages are MIT and pass the CI license check:

  butterchurn ........... MIT (Copyright (c) 2013-2018 Jordan Berg) — WebGL Milkdrop
                          visualizer engine. Ship-safe.
  butterchurn-presets ... MIT (Copyright (c) 2013-2018 Jordan Berg). The package and its
                          LICENSE are MIT and cover the bundled presets. ⚠ NOTE for the
                          commercial-ship decision: the presets are community-authored
                          Milkdrop conversions; their MIT grant comes from this package, but
                          if stricter per-author provenance is required, replace the full pack
                          with a hand-picked MIT/CC0 subset (with attribution). The layer is
                          default-OFF and lazily loaded, so this decision can be made before any
                          shipping build turns it on. Wiring: app/src/render/ButterchurnLayer.ts.

Media assets in the shipped corpus each carry license, source, and (where required)
attribution metadata in the manifest, and CC-BY attributions are rendered on screen.
